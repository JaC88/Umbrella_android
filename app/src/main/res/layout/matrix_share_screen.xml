<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/choose_type"
        layout="@layout/matrix_share_radio_chooser"
        android:visibility="gone" />

    <include
        android:id="@+id/matrix_sign_in"
        layout="@layout/matrix_share_item" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/send_item_button"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:layout_marginLeft="@dimen/text_size_larger"
        android:layout_marginRight="@dimen/text_size_larger"
        android:layout_marginBottom="100dp"
        android:background="@drawable/rounded_rectangle_grey"
        android:text="SEND"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>